import { Text, View } from "react-native";
import { styles } from "./Style";

export default function VerifyOTP() {
    return (
        <View style={styles.container}>
            <Text style={styles.text}>VerifyOTP Screen</Text>
        </View>
    );
}